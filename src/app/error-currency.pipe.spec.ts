import { ErrorCurrencyPipe } from './error-currency.pipe';

describe('ErrorCurrencyPipe', () => {
  it('create an instance', () => {
    const pipe = new ErrorCurrencyPipe();
    expect(pipe).toBeTruthy();
  });
});
